{% extends 'admin/base.html.twig' %}

{% block content %}
        {% for message in app.flashes('success') %}
                <div class="row mt-3">
                        <div class="col">
                                <div class="alert alert-success">
                                        {{ message }}
                                </div>
                        </div>
                </div>
        {% endfor %}
        <a href="{{ path('customPage_new') }}">Create new custom page</a>
{% endblock %}