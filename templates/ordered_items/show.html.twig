{% extends 'base.html.twig' %}

{% block title %}OrderedItems{% endblock %}

{% block body %}
    <h1>OrderedItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordered_item.id }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ ordered_item.adress }}</td>
            </tr>
            <tr>
                <th>Paid</th>
                <td>{{ ordered_item.paid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordered_items_index') }}">back to list</a>

    <a href="{{ path('ordered_items_edit', {'id': ordered_item.id}) }}">edit</a>

    {{ include('ordered_items/_delete_form.html.twig') }}
{% endblock %}
